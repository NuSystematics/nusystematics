set(CONFIG_INSTALL_ARGS FILES_MATCHING PATTERN "*.yaml")
if(NOT nusyst_DUNETDRv1_ENABLED)
    list(APPEND CONFIG_INSTALL_ARGS PATTERN "DUNE_TDRv1" EXCLUDE)
endif()

install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/
        DESTINATION ${CMAKE_INSTALL_PREFIX}/config
        ${CONFIG_INSTALL_ARGS})
