file(GLOB_RECURSE FCLFILES ${CMAKE_SOURCE_DIR}/fcl/*.fcl)

if(NOT nusyst_DUNETDRv1_ENABLED)
    list(FILTER FCLFILES EXCLUDE REGEX "/DUNE_TDRv1/")
endif()

install(FILES ${FCLFILES} DESTINATION ${CMAKE_INSTALL_PREFIX}/fcl)